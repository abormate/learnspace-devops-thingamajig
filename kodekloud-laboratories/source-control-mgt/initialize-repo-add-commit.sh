# create and initialize repo on pwd for this example named "myrepo"
git init myrepo

# check status for untracked files once inside repo folder
git status

# cd into repo directory
cd /home/thor/myrepo

# add untracked file into git
git add index.html

# commit file into git with a custom commit message -- fill out the commit message with something meaningful and descriptive
git commit -m 'your commit message'
