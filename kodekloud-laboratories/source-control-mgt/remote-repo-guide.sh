# download an entire remote repo into a local environment
git clone <url of remote repo like on github>

# command git pull updates a local repo with changes made to a remote repo without downloading the entire remote repo once again
git pull

