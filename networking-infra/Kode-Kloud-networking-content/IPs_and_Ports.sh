# linux comm shows the IP addresses and details of connected interfaces in the network
ip addr show 

# Ports are comms end-point that can have 65,535 ports for each IP address. And programs can listen to a particular port -- for the example below, it is any IP address for the port 8080
0.0.0.0:8080

# loopback mirror interface IP (or localhost)
127.0.0.1
