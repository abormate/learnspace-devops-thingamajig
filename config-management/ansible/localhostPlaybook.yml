---
  - name: "Challenge practice"
    hosts: localhost

    tasks:

      - name: "test reachability"
        ping:

      - name: "find module"
        find:
          path: ~/Users/a/documents/DevOps/ansible
          file_type: file

